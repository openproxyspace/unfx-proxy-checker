.ping-judges-container {
    position: fixed;
    display: flex;
    flex-flow: column nowrap;
    align-items: center;
    padding: 2em;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    overflow-y: auto;
    background-color: color(#fff alpha(95%));
    transition: opacity 0.7s easeOne, visibility 0.7s easeOne, z-index 0.7s easeOne;
    opacity: 0;
    visibility: hidden;
    z-index: -1;

    &.opened {
        opacity: 1;
        z-index: 3;
        visibility: visible;

        & .ping-judges-content {
            visibility: visible;
            opacity: 1;
            transform: translateY(0%);
        }
    }

    & .ping-judges-content {
        max-width: 960px;
        width: 100%;
        opacity: 0;
        transform: translateY(-10%);
        transition: opacity 0.7s easeOne, transform 0.7s easeOne;;

        & .ping-progress {
            display: inline-flex;
            font-size: 1em;
            font-weight: 700;
            user-select: none;
            border-radius: 1.75em;
            padding: 0.5em 1.75em;
            border: 1px solid transparent;
            color: color(var(--grey-color) alpha(75%));
            background-color: color(var(--grey-color) alpha(15%));
        }

        & .items {
            border-top: 1px solid #fafafa;
            margin-top: 2em;
        }

        & .item {
            display: flex;
            flex-flow: row wrap;
            padding: 1.15em 0;
            height: 4em;
            align-items: center;
            font-weight: 700;
            transition: color 0.7s easeOne;
            color: color(var(--grey-color) alpha(65%));
            border-bottom: 1px solid #fafafa;

            & svg {
                width: 1.25em;
                height: 1.25em;
                margin-right: 0.75em;
                fill: color(var(--grey-color) alpha(65%));
                transition: fill 0.7s easeOne;
            }

            &.success {
                color: color(var(--green-color) alpha(65%));

                & svg {
                    fill: color(var(--green-color) alpha(65%));
                }

                & .response {
                    color: #fff;
                    background-color: color(var(--green-color) alpha(65%));
                }
            }

            &.error {
                color: color(var(--red-color) alpha(65%));

                & svg {
                    fill: color(var(--red-color) alpha(65%));
                }

                & .response {
                    color: #fff;
                    background-color: color(var(--red-color) alpha(65%));
                }
            }

            &:last-child {
                border-bottom: none;
            }

            & .url {
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }

            & .status {
                margin-left: auto;

                & svg path {
                    fill: color(var(--grey-color) alpha(65%));
                }

                & .response {
                    display: inline-block;
                    padding: 0.35em 0.75em;
                    border-radius: 1em;
                    font-size: 0.85em;
                    animation: fade-judge-status 0.7s easeOne;
                    text-align: center;
                }
            }
        }
    }
}

@keyframes fade-judge-status {
    from {
        opacity: 0;
        transform: translateY(50%);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
